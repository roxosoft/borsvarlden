@using borsvarlden.ViewModels;
@model List<NewsViewModel>

<div class="read-more-body">
    <p class="label read-more-label">
        LÄS MER
    </p>
    <div class="owl-carousel owl-theme read-more-container">
        @foreach (var article in Model)
        {
            <div class="read-more-wrapper">
                <a href="~/artiklar/@Html.Raw(article.TittleSlug)" class="read-more-link">
                    <div class="read-more-image">
                        @if (string.IsNullOrEmpty(article.ImageUrl))
                        {
                            <img src="~/assets/images/borsvarlden_logo_alternative_510x510.png">
                        }
                        @if (!string.IsNullOrEmpty(article.ImageUrl))
                        {
                            <img src="@article.ImageUrl">
                        }
                    </div>
                    <div class="read-more-content">
                        <p>@article.Title</p>
                    </div>
                </a>
            </div>
        }
    </div>
</div>
